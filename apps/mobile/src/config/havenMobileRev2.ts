/**
 * haven-rev2 shell flag. Import from here so `App.tsx` and `RootNavigator` stay aligned.
 * Toggle while developing the drawer rebuild (see `src/haven-rev2/README.md`).
 */
export const USE_HAVEN_REV2 = __DEV__ && true;
