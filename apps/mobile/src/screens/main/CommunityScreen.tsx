/**
 * @deprecated Navigation uses `CommunityShell` + `CommunityChatScreen`.
 * Kept temporarily so imports do not break during the mobile nav migration.
 */
export { CommunityChatScreen as CommunityScreen } from "@/screens/main/CommunityChatScreen";
