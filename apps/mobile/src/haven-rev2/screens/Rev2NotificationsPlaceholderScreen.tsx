import { Box } from "@/components/ui/box";
import { Text } from "@/components/ui/text";

export function Rev2NotificationsPlaceholderScreen() {
  return (
    <Box className="flex-1 justify-center bg-background px-6">
      <Text className="text-foreground">Notifications placeholder — wire inbox parity later.</Text>
    </Box>
  );
}
