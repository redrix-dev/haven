import { Box } from "@/components/ui/box";
import { Text } from "@/components/ui/text";

export function Rev2SettingsPlaceholderScreen() {
  return (
    <Box className="flex-1 justify-center bg-background px-6">
      <Text className="text-foreground">Settings placeholder — full settings stack later.</Text>
    </Box>
  );
}
